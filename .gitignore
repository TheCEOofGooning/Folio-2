# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# production
/build

# local database (PGlite data directory used by `npm run dev:db`)
/.data

# env files — `.env.example` is committed, everything else is not
.env
.env.local
.env.*.local

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-error.log*
.vercel
